[project]
name = "opa-runtime"
version = "0.0.1"
description = "Kernel-side runtime shim for open-primeagent (rlm / harness / goal / agent_message)"
requires-python = ">=3.11"
dependencies = ["nest-asyncio>=1.6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/opa_runtime"]
