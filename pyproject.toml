[project]
name = "open-primeagent"
version = "0.0.1"
description = "RLM (persistent Python + persistent subagents + continual harness) for the coding agent you already use"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }

dependencies = [
  "mcp>=1.2",           # MCP server (stdio)
  "jupyter-client>=8",  # drives the persistent IPython kernel
  "ipykernel>=6",       # the kernel itself
  "opa-runtime",        # kernel-side shim (workspace member)
]

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-asyncio>=0.24", "ruff>=0.6"]

[project.scripts]
opa = "opa.server:main"
opa-child = "opa.child_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/opa"]

[tool.uv.workspace]
members = ["runtime"]

[tool.uv.sources]
opa-runtime = { workspace = true }

[tool.ruff]
line-length = 100
src = ["src", "runtime/src"]
# _ref: upstream clone, read-only. examples: kernel cell code, uses top-level await.
extend-exclude = ["_ref", "examples"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
  "slow: integration tests that boot a real IPython kernel",
  "child: spawns a real coding-agent CLI as a child (needs auth and token quota)",
]
addopts = "-m 'not child'"   # child tests run only when asked for with -m child

[dependency-groups]
dev = [
    "pytest-cov>=7.1.0",
]
